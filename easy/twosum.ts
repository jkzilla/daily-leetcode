
var twoSum = fucntions(SVGAnimatedNumberList, target) {
    for (let i = 0; i < array.length; i++) {
        if (nums[j] === target-nums[i]) {
            console.log([i,j]);
        }
    }
    return[];